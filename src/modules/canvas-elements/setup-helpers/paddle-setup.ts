// import
import { paddleImg } from "../../../images/images";

// defining width, height, startposition and speed of paddle
const defaultPaddleWidth = 150;
const paddleHeight = 25;
const paddleStartX = 450;
const defaultPaddleSpeed = 10;

export { paddleImg, defaultPaddleWidth, paddleHeight, paddleStartX, defaultPaddleSpeed };
