const canvas: HTMLCanvasElement | null = document.querySelector('#playfield-canvas');
const currentScore: HTMLElement | null = document.querySelector('#current-score');
const startBtn: HTMLButtonElement | null = document.querySelector('#start-button');
const playerInfo: HTMLElement | null = document.querySelector('#player-info');


export { canvas, currentScore, startBtn, playerInfo }