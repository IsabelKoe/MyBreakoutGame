import { ballImg } from "../../images/Images";
// defining size, speed and start position of ball
const ballSpeed = 5;
const ballSize = 20;
const balltXStartPos = 500;
const ballYStartPos = 400;

export {ballImg, ballSpeed, ballSize, balltXStartPos, ballYStartPos}
